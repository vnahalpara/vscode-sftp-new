# vite@6 peer-wants @types/node ^18||^20||>=22, but this repo deliberately
# pins @types/node ^9.6.51 because the extension targets an old VS Code
# Node runtime. That peer conflict is expected and safe to ignore.
legacy-peer-deps=true
